{
  "name": "DataExpress",
  "version": "0.1.0",
  "description": "数据特快专递服务端",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . --platform=win32 --arch=x64 --overwrite --asar --out=bin --icon=src/assets/app/images/ra.ico --ignore=node_modules --ignore=资源 --ignore=.gitignore --ignore=.project"
  },
  "devDependencies": {
    "electron-prebuilt": "^1.2.0",
    "electron-packager": "~7.7.0",
    "internal-ip": "*"
  }
}